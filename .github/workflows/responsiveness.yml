name: Responsiveness Action

on: [push]

jobs:
  responsiveness_job:
    runs-on: ubuntu-latest
    name: Check responsiveness score of repo
    steps:
      - name: Responsivenesss calculation and output
        id: responsiveness
        uses: pavitthrap/hci-responsiveness-action@v1.4.0
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          number-of-months: 1
          repo-name: "hci-demo"

